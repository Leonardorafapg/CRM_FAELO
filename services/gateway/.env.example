# URL interna do platform-service — em dev e uma porta local; em producao
# seria um hostname interno (nunca exposto publicamente).
PLATFORM_SERVICE_URL=http://localhost:8001

# Quem pode chamar o gateway a partir do browser (o frontend).
ALLOWED_ORIGINS=http://localhost:3000
